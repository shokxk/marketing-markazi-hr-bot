{
  "name": "marketing-markazi-hr-bot-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "cd backend && npm install",
    "build": "cd backend && npx prisma generate && npm run build",
    "start": "cd backend && npx prisma db push && npx ts-node prisma/seed.ts && node dist/index.js"
  }
}
